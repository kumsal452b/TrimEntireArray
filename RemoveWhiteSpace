for (var i = 0; i < Object.keys(res).length; i++) {
            if (
              res[Object.keys(res)[i]] !== null &&
              typeof res[Object.keys(res)[i]] !== 'number'
            ) {
              res[Object.keys(res)[i]] = res[Object.keys(res)[i]]?.trim();
  ]}
}
