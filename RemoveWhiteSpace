 removeWhiteSpace(array:any){
    for (var i = 0; i < Object.keys(array).length; i++) {
      if (
        array[Object.keys(array)[i]] !== null &&
        typeof array[Object.keys(array)[i]] !== 'number'
      ) {
        array[Object.keys(array)[i]] = array[Object.keys(array)[i]]?.trim();
      }
    }
  }
